import React from 'react';

const PeriodTitle = (props) => {
  
    return (
        <div className="period-title"><button className="btn-period">{props.title}</button></div>
    );

}

export default PeriodTitle;